import Select from "./Select";

const SortBy = ({ options }) => {
  const handleChange() {}
  return <Select type="white" options={options} onChange={handleChange}></Select>;
};

export default SortBy;
